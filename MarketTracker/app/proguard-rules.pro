# Add project specific ProGuard rules here.
# This app ships with minifyEnabled = false by default (no obfuscation needed
# for a personal, local-only app), so this file is a placeholder for if that
# ever changes.
